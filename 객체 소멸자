객체 소멸자 - finalize()
GC(가비지 컬렉터)는 객체를 소멸하기 직전에 객체 소멸자를 실행한다.
Object의 finalize()는 기본적으로 실행 내용이 없다.

객체가 소멸되기 전에 실행할 코드가 있다면 Object의 finalize()를 재정의해야함

될 수 있으면 소멸자는 사용하지 않는게 좋다.
가비지 컬렉터는 메모리의 모든 쓰레기 객체를 소멸하지 않을뿐더러 구동시점이 일정하지 않기 때문이다.
