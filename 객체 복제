객체 복제 - clone()
원본 객체의 필드값과 동일한 값을 가지는 새로운 객체를 생성하는 것
원본 객체를 보호하기 위해 복제를 하는 것임

복제의 종류에는 얕은 복제와 깊은 복제 두 가지가 있다.
얕은 복제 - 필드값만 복제한다. 
깊은 복제 - 참조하고 있는 객체도 복제한다.

원본 객체를 얕은 복제하면 배열객체가 생기며 복제객체도 배열객체를 참조한다.
깊은 복제는 배열객체를 공유하지 않고 원본객체의 배열객체 따로 복제객체의 배열객체 따로 만들어주고 싶을 때 사용한다. 그래서 원본 객체와 복제 객체는 참조하는 객체가 다르다.

Object의 colne() 메소드는 동일한 필드값을 가진 얕은 복제된 객체를 리턴한다.
java.lang.Clonealbe 인터페이스를 구현한 객체만 복제 가능하다.
깊은 복제는 colne() 메소드를 재정의하고 참조 객체도 복제해야한다.
